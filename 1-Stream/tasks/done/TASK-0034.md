---
id: TASK-0034
title: Update generate prompt feature to use new template structure
type: task
status: done
priority: medium
rank: 1
owner: owner-name
assigned_to: Gary
created: 2026-03-24
updated: 2026-03-24
tags: ["ai", "prompting", "templates"]
related: []
---

## Summary
Update the generate prompt feature so that AI chatbots produce task files using the new simplified template structure instead of the previous format.

## Why
The current generate prompt logic still instructs AI to use the old template layout, which leads to inconsistent task files and breaks alignment with the updated system. Ensuring all generated tasks follow the new structure maintains consistency and prevents parsing or UI issues.

## Acceptance Criteria
- Generated prompts explicitly instruct AI to use the new template structure
- Output from AI-generated tasks matches the updated `work-item-template.md`
- No deprecated fields (status, owner, assigned_to, tags, related, etc.) appear in generated files
- Generated tasks are valid markdown and correctly parsed by the application
- Existing generate prompt UI flow continues to function without errors

## Context
Stream includes a feature that generates prompts for external AI tools to create task files. This prompt currently references an outdated template format, causing mismatches with the system's current expectations.

## Notes
- Update prompt text to reflect the new template fields and sections
- Ensure formatting instructions are strict enough to prevent drift
- Consider including an example output in the prompt for better consistency
- Validate generated files against parser before allowing import

## Activity Log
- 2026-03-24: Task created
- 2026-03-24: Moved task to doing and updated the generated prompt plus import parsing so AI-generated work items target the simplified template and still import cleanly into the app.
- 2026-03-24: Verified with `npm run typecheck` and `npm run build`.
